-- SMART RH 4.0 — MySQL init
-- This file runs once when the MySQL container is first created.
-- Flyway migrations (db/migration/V*.sql) handle all schema/data after this.

SET NAMES utf8mb4;
SET character_set_client = utf8mb4;
